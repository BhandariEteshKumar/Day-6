let a = prompt("Enter a number?");
//Don't modify any code below this
if (a) {
  console.log("OMG it works for any number inc 0");
} else {
  console.log("Success");
}

// Any value that is not false, undefined, null, 0, -0, NaN, or the empty string (""),
// and any object, including a Boolean object whose value is false, is considered truthy when used as the condition.
// If we enter empty input we can get Success
